<nav class="navbar fixed-top navbar-expand-lg navbar-dark ">
    <div class="container-fluid">
        <a class="navbar-brand" href="/">What to watch</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mx-auto mb-2 mb-lg-0">
                <% const navItems = [{name: 'Películas', route: '/movies'}, {name:'Series', route: '/shows'}, {name:'Lo mas visto', route: '/trending'}] %>
                <% navItems.forEach( (item) => { %>
                <li class="nav-item">
                    <a class="nav-link <%= route === item.route ? 'active' : '' %>" aria-current="page"
                        href="<%= item.route %>"><%= item.name %></a>
                </li>
                <% }); %>
            </ul>
        </div>
    </div>
</nav>